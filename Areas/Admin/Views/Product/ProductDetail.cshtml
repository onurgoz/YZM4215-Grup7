@model ProductViewModel

@section Styles{
  <link rel="stylesheet" href="/css/productdetail.css">
}


<div class="container">
    <div class="row">
        <div class="card mb-3">
            <getproductimage id="@Model.Id"></getproductimage>
            <div class="card-body">
                <h5 class="card-title">@Model.Name</h5>
                <p class="card-text">@Model.Description</p>
            </div>
        </div>
    </div>    
</div>

